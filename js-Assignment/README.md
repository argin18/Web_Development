# This is javascript related some assignment.

## QUENSTION ARE BELOW DOWN:-

1. Here is a list of items. Add a remove button for every item. With every click of the remove button, remove one item from the list.

2. Create an input field and take string input in it from the user. Render the input value on the viewport. Create a button and on click of the button reverse the input and display it on the view

3. Given the below data for clothing stock. (data available on js file

4. Write a JavaScript program to add items to favorites from the drop down list. If you select any item that should be added to the list and be displayed on the ui, if you click on the added item again then it should not add the same item again.

5. Write a JavaScript program to highlight the word with more than 5 characters with green background.

6. Given a table with 3 rows and 3 columns Write a JavaScript program to add a row and every click on button.

7. Given an input string S that contains multiple words, you need to remove all the spaces present in the input string. There can be multiple spaces present after any word

8. Given an object obj, we have to invert its key and value for each entry. Inversion will result in value being used as key and key as value.

9. Given an array of numbers, we have to find the number with the most frequency and return it. If there are two or more numbers with the same most frequency then return the biggest number.

10. Write a Javascript program to create a Rating Component ?
